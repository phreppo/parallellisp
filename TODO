[possima] cambiare il modo di avere parallelismo: modello serviroti (consapevoli) e server
[se necessario] mandare un nuovo thread solo valuto qualcosa che non e un atomo
[alla fine] analisi semantica doppo il parsing per vedere che le espressioni contengano side effect sul global solo al toplevel
[alla fine] provare un array invece diuna slice per gli argomenti se va piu veloce
[alla fine] mettere le freccie per canali di input e di output